import {StyleSheet} from "react-native"

const style = StyleSheet.create({
    userImage: {
      width: 45, 
      height: 45,
      margin: 10,
      borderRadius: 30
    },

    header: {
        flexDirection: "row",
        alignItems: "center",
    }
  })

export default style;